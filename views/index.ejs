<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Quora Post</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <h1>Quora Post</h1>
    <hr>
    <br>
     <button><a href="http://localhost:8000/post/new">create new post</a></button>
    <div class="container">
      <% for(post of posts) {%>
      <div class="post-card">
        <h3 class="username">@<%= post.username %></h3>
        <hr />
        <p class="content"><%= post.content %></p>
        <button><a href="http://localhost:8000/post/<%= post.id %>">See in detail</a></button>
         <button><a href="http://localhost:8000/post/<%= post.id %>/edit">Edit</a></button>
          <form method="POST" action="/post/<%= post.id %>?_method=DELETE">
            <button type="submit">Delete</button>
      </div>
      <% } %>
    </div>
  </body>
</html>
